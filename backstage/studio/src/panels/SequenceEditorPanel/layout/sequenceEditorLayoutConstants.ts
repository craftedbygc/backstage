/** Extra vertical space so the last dope-sheet row can scroll above the zoom scrollbar. */
export const SEQUENCE_EDITOR_BOTTOM_SCROLL_SLACK_PX = 24

/** Timeline inset from the left/right split in floating panels. */
export const SEQUENCE_EDITOR_SCALED_SPACE_LEFT_PADDING = 10

/** Mirror of {@link SEQUENCE_EDITOR_SCALED_SPACE_LEFT_PADDING} at the sequence end. */
export const SEQUENCE_EDITOR_SCALED_SPACE_RIGHT_PADDING = 10

/** Slightly larger inset in docked mode so keyframe/easing UI does not crowd the divider. */
export const SEQUENCE_EDITOR_DOCKED_SCALED_SPACE_LEFT_PADDING = 18

/** Mirror of {@link SEQUENCE_EDITOR_DOCKED_SCALED_SPACE_LEFT_PADDING} at the sequence end. */
export const SEQUENCE_EDITOR_DOCKED_SCALED_SPACE_RIGHT_PADDING = 18
